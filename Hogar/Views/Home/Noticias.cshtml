@{
    ViewData["Title"] = "Noticias";
}

<div class="hero-section text-white text-center py-5" style="background-color: #96B012; border-radius: 10px;">
    <div class="container">
        <h1 class="text-center fw-bold" style="color: #ffff; text-shadow: 2px 2px 4px black;">
            NOTICIAS
        </h1>
        <p class="lead" style="color: #fff;">Mantente al día con las últimas noticias y eventos de nuestra comunidad.</p>
    </div>
</div>

<div class="container mt-5">
    <div class="row">
        @* Simulación de Noticias dinámicas (se reemplazarán con datos del modelo) *@
        @for (int i = 0; i < 6; i++)
        {
            <div class="col-md-4 mb-4">
                <div class="card news-card shadow-lg border-0">
                    <div class="card-img-container">
                        <img src="/images/news-placeholder.jpg" class="card-img-top img-fluid" alt="Noticia" style="height: 200px; object-fit: cover;">
                        <div class="card-overlay text-white d-flex align-items-center justify-content-center">
                            <h5 class="card-title text-center fw-bold px-2">
                                Título de la Noticia Ejemplo
                            </h5>
                        </div>
                    </div>
                    <div class="card-body">
                        <p class="card-text justified-text" style="color: #4A4A42;">
                            Aquí irá un breve resumen de la noticia. Este texto debe ser lo suficientemente corto y atractivo para invitar al usuario a leer más.
                        </p>
                        <a href="#" class="btn btn-sm" style="background-color: #BAD535; color: white;">Leer más</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    /* Estilo de las noticias */
    .news-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        border-radius: 12px;
        overflow: hidden;
    }

        .news-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }

    .card-img-container {
        position: relative;
    }

    .card-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        opacity: 0;
        transition: opacity 0.3s ease;
        border-radius: 12px 12px 0 0;
        text-shadow: 1px 1px 4px black;
    }

    .news-card:hover .card-overlay {
        opacity: 1;
    }

    .justified-text {
        text-align: justify;
    }

    .btn {
        border-radius: 5px;
        font-weight: bold;
    }

        .btn:hover {
            background-color: #96B012;
            color: white;
        }
</style>
